/**
 * Public type barrel. Prefer importing from `@/types/domain` in new code.
 * Kept for backward compatibility with existing pages.
 */
export type {
  BlogPost,
  DailyLogEntry,
  FoodEntry,
  FoodEntryShort,
  Gender,
  Goal,
  MacroKey,
  ReminderSettings,
  SavedDietChart,
  UserProfile,
} from "@/types/domain";
